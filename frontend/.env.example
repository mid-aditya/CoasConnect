# CoasConnect Web — konfigurasi environment
# Selama development, Vite sudah mem-proxy /api ke http://localhost:8080,
# jadi tidak wajib mengisi variabel ini.

# URL API backend Go (dipakai saat build produksi)
# VITE_API_URL=http://localhost:8080
