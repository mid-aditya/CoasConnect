# CoasConnect API — konfigurasi environment
# Salin file ini menjadi .env lalu sesuaikan nilainya.

# Port HTTP server (default: 8080)
PORT=8080

# Lokasi file database SQLite (default: coasconnect.db)
DB_PATH=coasconnect.db
